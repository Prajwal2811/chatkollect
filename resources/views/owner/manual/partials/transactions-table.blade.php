@php($numberField = $type === 'sale' ? 'sale_number' : 'receipt_number')
@php($dateField = $type === 'sale' ? 'sale_date' : 'receipt_date')
<div class="table-responsive"><table class="table table-striped align-middle mb-0"><thead><tr><th>#</th><th>{{ $type === 'sale' ? 'Sale No.' : 'Receipt No.' }}</th><th>Amount</th><th>Date</th><th class="text-end">Action</th></tr></thead><tbody>@forelse ($items as $item)<tr><td>{{ $loop->iteration }}</td><td>{{ $item->{$numberField} }}</td><td>₹{{ number_format($item->amount, 2) }}</td><td>{{ $item->{$dateField}->format('d-m-Y') }}</td><td class="text-end"><button class="btn btn-outline-primary btn-sm transaction-edit" data-bs-toggle="modal" data-bs-target="#{{ $type }}Modal" data-type="{{ $type }}" data-label="{{ ucfirst($type) }}" data-number-field="{{ $numberField }}" data-date-field="{{ $dateField }}" data-number="{{ $item->{$numberField} }}" data-amount="{{ $item->amount }}" data-date="{{ $item->{$dateField}->format('Y-m-d') }}" data-update-url="{{ route('owner.manual.students.' . $type . 's.update', [$student, $item]) }}"><i class="fas fa-edit"></i></button><button class="btn btn-outline-danger btn-sm transaction-delete" data-type="{{ $type }}" data-delete-url="{{ route('owner.manual.students.' . $type . 's.destroy', [$student, $item]) }}"><i class="fas fa-trash"></i></button></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-4">No {{ $type }}s found.</td></tr>@endforelse</tbody></table></div>
